<footer class="glass-panel border-t border-gray-200 dark:border-gray-700 mt-12">
    <div class="max-w-5xl mx-auto px-4 sm:px-6 lg:px-8 py-8">
        <div class="text-center">
            <p class="text-gray-600 dark:text-gray-400 mb-2">
                <%= theme.footer %>
            </p>
            <% if (theme.beian) { %>
                <p class="text-gray-500 dark:text-gray-500 text-sm"><%- theme.beian %></p>
                <% } %>
        </div>
    </div>
</footer>

<!-- 回到顶部按钮 -->
<button id="back-to-top"
    class="fixed bottom-8 right-8 glass-panel text-gray-600 dark:text-gray-300 hover:text-primary-600 dark:hover:text-primary-400 p-3 rounded-full shadow-lg transition-all duration-300 opacity-0 invisible z-50">
    <%- icon('arrow-up', 'w-5 h-5' ) %>
</button>